#pragma once
#include <SFML/Graphics.hpp>
#include <SFML/Audio.hpp>

class Input
{
public:
	
	enum states
	{
		left,
		right,
		jump
	};

	void CheckForInput(sf::Window &window);
	void UseInput(+)
	
};

